Github sending Normal objects 
git remote add origin https://github.com/Bunnydavid27/Spotify_data_analy_aws_ETL.git
git add .
git commit -m "updated"
git push origin master


Github Large files Sending 
git lfs install
git lfs track "data/*"
git lfs migrate import --include="*.csv"
same steps of normal pushing objects into the Github repo

